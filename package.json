{
  "name": "bluekaiChallenge",
  "version": "0.0.0",
  "description": "I wrote a method in this repository that right and left justifies a string that is passed into the method formatting the body of text to conform to a specified margin length.",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex Miranda",
  "license": "ISC",
  "devDependencies": {
    "grunt": "^0.4.4",
    "grunt-contrib-jshint": "^0.10.0",
    "mocha": "^1.18.2",
    "chai": "^1.9.1",
    "grunt-mocha": "^0.4.10",
    "grunt-simple-mocha": "^0.4.0"
  }
}
